---
//Notas agregar :
//1.  Importar Estilos.
//2.  Importar Componentes.
//3. aplicar precarga de midelwares de autenticacion.
---
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login Eduam</title>

<!-- ************************************************** -->
    <!-- TAILWIND Y FLOWBITE DEBEN IR IMPORTADOS DESDE SU ARCHIVO DE ESTILOS CORRESPONDIENTES -->
     <!-- los estilos de tailwind y flowbite deben ir en el archivo de estilos de tailwind y flowbite dado que para eso estan las plantillas y componentes!!! -->
<!-- ************************************************** -->

    <!-- CDN de Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- CDN de Flowbite CSS -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/flowbite/2.2.1/flowbite.min.css" rel="stylesheet" />





<!-- ************************************************** -->
    <!-- ESTOS ESTILOS SON NO DEBEN IR EN ESTE ARCHIVO -->
<!-- ************************************************** -->
   
   <style>
        /* Solo animaciones que no están disponibles en Tailwind */
        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-10px); }
        }
        
        @keyframes slideInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .animate-float {
            animation: float 6s ease-in-out infinite;
        }

        .animate-slide-in-up {
            animation: slideInUp 0.6s ease-out;
        }
    </style>




</head>
<body class="min-h-screen w-screen relative bg-blue-200 font-sans overflow-hidden">
    <!-- Contenedor del fondo con imagen y superposición de degradado -->
    <div class="absolute inset-0 z-0">
        <!-- Imagen de fondo con filtro azul -->
        <img src="../../../imgs/estudiantes.jpg" alt="Fondo estudiantes" class="w-full h-full object-cover blur-3xl" />
        <!-- Superposición con filtro azul para enfocar el contenido -->
        <div class="absolute inset-0 bg-gradient-to-br from-blue-600/80 via-blue-700/70 to-blue-800/60"></div>
        <!-- Efecto de partículas flotantes -->

    </div>

    <!-- Contenido principal centrado en la pantalla -->
    <div class="relative z-10 flex flex-col items-center justify-center min-h-screen px-4 py-8">
        <!-- Sección del escudo y la frase -->
        <div class="flex flex-col items-center mb-8 text-center animate-slide-in-up">
            <!-- Escudo con efecto flotante -->
            <div class="animate-float mb-4 ">
                <img src="../../../imgs/escudo-alm.webp" alt="Escudo Colegio" class="w-32 md:w-40 drop-shadow-2xl rounded-full border-4 border-white/20" />
            </div>
            <div class="bg-blue-50 backdrop-blur-md border border-white/20 px-6 py-3  rounded-sm">
                <p class=" text-lg md:text-xl font-bold tracking-wide text-blue-800">"UNA VIDA PARA LA EXCELENCIA"</p>
            </div>
        </div>

        <!-- Contenedor del formulario de inicio de sesión -->
        <div class="backdrop-blur-xl  bg-white border border-white shadow-2xl p-8 md:p-10 rounded-lg w-full max-w-sm md:max-w-md animate-slide-in-up" style="animation-delay: 0.2s;">
            <div class="text-center mb-8">
                <h2 class="text-3xl font-bold text-gray-800 mb-2">Bienvenido</h2>
                <p class="text-gray-600">Ingresa tus credenciales para continuar</p>
            </div>

            <!-- Formulario de acceso -->
            <form id="loginForm" class="space-y-6">
                <!-- Campo de entrada para el usuario (correo) -->
                <div class="relative group">
                    <input 
                        type="email" 
                        id="correo_usuario" 
                        name="correo_usuario" 
                        class="peer block w-full appearance-none border-2 border-gray-200 bg-white/90 px-4 pb-3 pt-6 text-sm text-gray-900 rounded-xl focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent focus:bg-white transition-all duration-300 ease-in-out hover:bg-white focus:transform focus:-translate-y-0.5 focus:shadow-lg" 
                        placeholder=" " 
                        required 
                    />
                    <label 
                        for="correo_usuario" 
                        class="absolute text-sm text-gray-500 duration-300 transform -translate-y-4 scale-75 top-4 z-10 origin-[0] left-4 peer-placeholder-shown:scale-100 peer-placeholder-shown:translate-y-2 peer-focus:scale-75 peer-focus:-translate-y-4 peer-focus:text-blue-600 transition-all ease-in-out"
                    >
                        <i class="fas fa-envelope mr-2"></i>Correo Electrónico
                    </label>
                    <p id="error-correo_usuario" class="text-red-500 text-sm mt-1 hidden animate-pulse"></p>
                </div>

                <!-- Campo de entrada para la contraseña -->
                <div class="relative group">
                    <input 
                        type="password" 
                        id="contraseña_usuario" 
                        name="contraseña_usuario" 
                        class="peer block w-full appearance-none border-2 border-gray-200 bg-white/90 px-4 pb-3 pt-6 text-sm text-gray-900 rounded-xl focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent focus:bg-white transition-all duration-300 ease-in-out hover:bg-white focus:transform focus:-translate-y-0.5 focus:shadow-lg" 
                        placeholder=" " 
                        required 
                    />
                    <label 
                        for="contraseña_usuario" 
                        class="absolute text-sm text-gray-500 duration-300 transform -translate-y-4 scale-75 top-4 z-10 origin-[0] left-4 peer-placeholder-shown:scale-100 peer-placeholder-shown:translate-y-2 peer-focus:scale-75 peer-focus:-translate-y-4 peer-focus:text-blue-600 transition-all ease-in-out"
                    >
                        <i class="fas fa-lock mr-2"></i>Contraseña
                    </label>
                    <p id="error-contraseña_usuario" class="text-red-500 text-sm mt-1 hidden animate-pulse"></p>
                </div>

                <!-- Enlace para recuperar contraseña -->
                <div class="flex justify-end">
                    <a href="contrasena" class="text-sm text-blue-600 hover:text-blue-800 hover:underline transition-colors duration-200">
                        ¿Olvidaste tu contraseña?
                    </a>
                </div>

                <!-- Botón de acceso -->
                <button 
                    type="submit" 
                    class="w-full bg-blue-500 text-white font-semibold rounded-xl text-sm px-6 py-4 text-center transition-all duration-300 transform hover:scale-105 hover:shadow-xl hover:bg-blue-800  active:scale-95 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-opacity-50"
                >
                    <span class="flex items-center justify-center">
                        <i class="fas fa-sign-in-alt mr-2"></i>
                        Iniciar Sesión
                    </span>
                </button>

                <!-- Separador -->
                <div class="relative my-6">
                    <div class="absolute inset-0 flex items-center">
                        <div class="w-full border-t border-gray-300"></div>
                    </div>
                    <!-- <div class="relative flex justify-center text-sm">
                        <span class="px-2 bg-white text-gray-500">O continúa con</span>
                    </div> -->
                </div>

                <!-- Botones sociales -->
                <div class="grid grid-cols-2 gap-3">
                    <!-- Botón de Google -->
                    <!-- <button type="button" class="flex justify-center items-center px-4 py-3 border border-gray-300 rounded-xl text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-blue-500 transition-all duration-200 transform hover:scale-105">
                        <svg class="w-5 h-5 mr-2" viewBox="0 0 24 24">
                            <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                            <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                            <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"/>
                            <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/>
                        </svg>
                        Google
                    </button> -->

                    <!-- Botón de Microsoft -->
                    <!-- <button type="button" class="flex justify-center items-center px-4 py-3 border border-gray-300 rounded-xl text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-blue-500 transition-all duration-200 transform hover:scale-105">
                        <svg class="w-5 h-5 mr-2" viewBox="0 0 24 24">
                            <path fill="#f25022" d="M0 0h11v11H0z"/>
                            <path fill="#7fba00" d="M12 0h11v11H12z"/>
                            <path fill="#00a4ef" d="M0 12h11v11H0z"/>
                            <path fill="#ffb900" d="M12 12h11v11H12z"/>
                        </svg>
                        Microsoft
                    </button> -->
                </div>

                <!-- Enlace para volver al inicio -->
                <div class="text-center mt-6">
                    <a href="/" class="text-sm text-gray-600 hover:text-blue-600 hover:underline transition-colors duration-200">
                        <i class="fas fa-arrow-left mr-1"></i>
                        Volver al inicio
                    </a>
                </div>
            </form>
        </div>
    </div>

    <!-- Modal para mensajes de estado mejorado -->
    <div id="statusModal" class="fixed inset-0 bg-black/50 backdrop-blur-sm flex items-center justify-center hidden z-50">
        <div class="bg-white dark:bg-slate-800 p-8 rounded-3xl shadow-2xl max-w-sm w-full transform transition-all scale-95 duration-300 border border-gray-200">
            <div class="text-center">
                <div id="modalIcon" class="text-6xl mb-4"></div>
                <h3 id="modalTitle" class="text-2xl font-bold mb-3 text-gray-900 dark:text-white"></h3>
                <p id="modalMessage" class="text-gray-700 dark:text-gray-300 mb-6"></p>
                <button id="closeModalBtn" class="bg-gradient-to-r from-blue-600 to-blue-700 text-white px-8 py-3 rounded-xl hover:from-blue-700 hover:to-blue-800 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-opacity-50 transition-all duration-200 transform hover:scale-105">
                    Cerrar
                </button>
            </div>
        </div>
    </div>

    <!-- Font Awesome para iconos -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    <!-- ************************************************** -->
    <!-- ESTOS SCRIPTS SON NO DEBEN IR EN ESTE ARCHIVO -->
    <!-- ************************************************** -->
    <!-- CDN de Flowbite JS (debe ir al final del body para un mejor rendimiento) -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/flowbite/2.2.1/flowbite.min.js"></script>

    <script>
// import { effect } from "astro:schema";

        const loginForm = document.getElementById('loginForm');
        const correoInput = document.getElementById('correo_usuario');
        const passwordInput = document.getElementById('contraseña_usuario');

        const statusModal = document.getElementById('statusModal');
        const modalIcon = document.getElementById('modalIcon');
        const modalTitle = document.getElementById('modalTitle');
        const modalMessage = document.getElementById('modalMessage');
        const closeModalBtn = document.getElementById('closeModalBtn');


        loginForm.addEventListener('submit', async (event) => {
            event.preventDefault();
            console.log('Formulario enviado');
            let loginData = {
                correo_usuario: correoInput.value.trim(),
                contraseña_usuario: passwordInput.value.trim()
            };
            console.log(loginData);

            fetch('http://localhost:3000/api/login', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify(loginData)
            })
            .then(response => response.json()) 
            .then(data => {
                console.log('Respuesta del servidor:', data);
                if(data.rol === 3){
                    // Redirigir a la página de administrador
                    window.location.href = '/estudiantes/perfilestudiante';
                }
                if(data.rol === 2){
                    // Redirigir a la página de administrador
                    window.location.href = '/docentes/perfildocente';
                }

            })
            .catch(error => {
                console.error('Error al enviar el formulario:', error);
            });

        })



            // if (!validateLoginForm()) {
            //     console.log('Validación de formulario fallida.');
            //     return;
            // }

            

            // Mostrar estado de carga
            // const submitBtn = loginForm.querySelector('button[type="submit"]') as HTMLButtonElement;
            // const originalText = submitBtn.innerHTML;
            // submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i>Iniciando sesión...';
            // submitBtn.disabled = true;

            // const loginData = {
            //     correo_usuario: correoInput.value.trim(),
            //     contraseña_usuario: passwordInput.value.trim()
            // };

            // try {
            //     const response = await fetch('http://localhost:3000/api/login', {
            //         method: 'POST',
            //         headers: {
            //             'Content-Type': 'application/json'
            //         },
            //         body: JSON.stringify(loginData)
            //     });

            //     const result = await response.json();

            //     if (response.ok) {
                    // Login exitoso
                //     const userRole = result.user.rol;
                //     const userName = result.user.nombres;
                //     const userLastName = result.user.apellidos;
                //     const jwtToken = result.token;

                //     // Almacenar el JWT y los datos del usuario en localStorage
                //     localStorage.setItem('jwtToken', jwtToken); 
                //     localStorage.setItem('userRole', userRole); 
                //     localStorage.setItem('userName', `${userName} ${userLastName}`);
                //     localStorage.setItem('userId', result.user.id);

                //     showModal('✅', '¡Inicio de Sesión Exitoso!', `Bienvenido, <strong>${userName} ${userLastName}</strong>. Tu rol es: <strong>${userRole}</strong>.`);
                //     loginForm.reset();

                //     // Redirigir al usuario al perfil después de un breve retraso
                //     setTimeout(() => {
                //         window.location.href = '/usuario/perfilUsuario';
                //     }, 1500);
                    
                // else {
                //     // Login fallido
                //     showModal('❌', 'Error de Inicio de Sesión', result.message || 'Ocurrió un error inesperado al intentar iniciar sesión.');
                // }
            // } catch (error) {
            //     console.error('Error de red o de solicitud:', error);
            //     showModal('⚠️', 'Error de Conexión', 'No se pudo conectar con el servidor. Por favor, intente de nuevo más tarde.');
            // } finally {
            //     // Restaurar botón
            //     submitBtn.innerHTML = originalText;
            //     submitBtn.disabled = false;
            // }
        // });

        // Efectos de hover para los campos de entrada
        // document.querySelectorAll('input[type="email"], input[type="password"]').forEach(input => {
        //     input.addEventListener('focus', function() {
        //         this.parentElement.classList.add('ring-2', 'ring-blue-500', 'ring-opacity-20');
        //     });
            
        //     input.addEventListener('blur', function() {
        //         this.parentElement.classList.remove('ring-2', 'ring-blue-500', 'ring-opacity-20');
        //     });
        // });
    </script>
</body>
</html>
